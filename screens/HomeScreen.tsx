import React from 'react';
import { Text, View } from 'react-native';

const HomeScreen = () => {
	return (
		<View style={{ flex: 1 }}>
			<Text style={{ color: '#fff' }}>Welcome home</Text>
		</View>
	);
};

export default HomeScreen;
